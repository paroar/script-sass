# script-sass
script for sass that creates 7:1 architecture folders and files


1-Move/copy sass71 file to /usr/bin => mv path/from/origin /usr/bin

2-Add executable permission to the sass71 file => sudo chmod +x /usr/bin/sass71

3-Now you can use the command "sass71" to create sass architecture 7-1
